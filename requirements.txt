# Pinned to exact versions: Vercel resolves this file fresh on every deploy,
# so an unpinned major release upstream (supabase, the provider SDKs, PTB)
# would land in production unreviewed. Bump deliberately and let CI run.

# Web Framework
fastapi==0.141.1
uvicorn==0.52.4
gunicorn==26.2.0
python-multipart==0.0.32

# Database
psycopg2-binary==2.9.12

# Authentication & secrets
supabase==2.31.0
python-jose==3.5.0
cryptography==50.0.1

# Telegram Bot
python-telegram-bot==22.8
telegramify-markdown==1.2.0

# AI/LLM Providers
google-genai==2.22.0
anthropic==1.3.0
openai==3.7.0

# Data Processing
pydantic==2.13.5
python-frontmatter==1.3.0

# HTTP Clients
requests==2.34.2
httpx==0.28.1

# Templates
jinja2==3.1.6

# Environment
python-dotenv==1.2.3

# Monitoring (activates only when SENTRY_DSN is set)
sentry-sdk[fastapi]==2.68.1

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
testcontainers==4.15.0
